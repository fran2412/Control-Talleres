<UserControl x:Class="ControlTalleresMVP.UI.Component.Clases.FormularioClaseUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:ControlTalleresMVP.UI.Component.Clases"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- Atajos -->
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding GuardarPagoClasesCommand}"/>
        <KeyBinding Modifiers="Control" Key="L" Command="{Binding LimpiarSeleccionCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding CancelarPagoClasesCommand}"/>
    </UserControl.InputBindings>

    <Grid Background="White">
        <Border Style="{StaticResource BorderGridBase}" Padding="16" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Header -->
                    <RowDefinition Height="Auto"/>
                    <!-- Alumno/Taller -->
                    <RowDefinition Height="*"/>
                    <!-- Tabla -->
                    <RowDefinition Height="Auto"/>
                    <!-- Notificación -->
                    <RowDefinition Height="Auto"/>
                    <!-- Resumen -->
                    <RowDefinition Height="Auto"/>
                    <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,8">
                    <TextBlock Text="Pago de clases"
                           FontSize="20" FontFamily="Inter 28pt 28pt ExtraBold"
                           Foreground="#3E3E3E" DockPanel.Dock="Left"/>
                    <TextBlock Text="{Binding FechaDeHoy}"
                           Foreground="#6B7280" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </DockPanel>

                <!-- ALUMNO / TALLER -->
                <WrapPanel Grid.Row="1" Margin="0,0,0,10">
                    <!-- Alumno -->
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="* Alumno" FontFamily="Inter 24pt 24pt Medium" FontSize="14" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding AlumnoNombre}" Style="{StaticResource TextBoxCampo}"
                                 IsReadOnly="True" Height="30" Cursor="Arrow"  Width="360" ToolTip="Alumno seleccionado"/>
                            <Button Command="{Binding BuscarAlumnoCommand}"
                                Style="{StaticResource SemiFeaturedButton}"
                                Height="40" Width="110" Margin="6,0,0,0" ToolTip="Buscar alumno">
                                <svgc:SvgViewbox Source="Resources/Icons/LupaIcono.svg" Width="28" Height="28"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <!-- Taller -->
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="* Taller" FontFamily="Inter 24pt 24pt Medium" FontSize="14" Margin="0,0,0,9"/>

                        <ComboBox ItemsSource="{Binding TalleresDelAlumno, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding TallerSeleccionado, Mode=TwoWay}"
                              DisplayMemberPath="Nombre"
                              Style="{StaticResource MinimalComboBoxStyle}"
                              Width="260" Height="30"
                              ToolTip="Selecciona el taller del alumno para hoy"/>
                    </StackPanel>
                </WrapPanel>

                <!-- TABLA DE CLASES -->
                <Border Grid.Row="2" Padding="12" CornerRadius="12" Background="#FFF9FAFB" BorderBrush="#EEE" BorderThickness="1" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Selecciona las clases que deseas cobrar"
                                   FontFamily="Inter 24pt 24pt Medium" FontSize="14"/>
                        
                        <Grid Grid.Row="2">
                            <DataGrid ItemsSource="{Binding ClasesParaPago}"
                                      AutoGenerateColumns="False"
                                      HeadersVisibility="Column"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      CanUserResizeRows="False"
                                      CanUserReorderColumns="False"
                                      Margin="0"
                                      GridLinesVisibility="None"
                                      RowHeaderWidth="0"
                                      SelectionMode="Extended"
                                      IsReadOnly="False">
                                <DataGrid.Resources>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Padding" Value="6"/>
                                    </Style>
                                </DataGrid.Resources>
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EstaHabilitada}" Value="False">
                                                <Setter Property="Opacity" Value="0.45"/>
                                                <Setter Property="ToolTip" Value="Paga primero las clases pendientes para habilitar esta fecha."/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Width="60">
                                        <DataGridTemplateColumn.Header>
                                            <TextBlock Text="Pagar" HorizontalAlignment="Center"/>
                                        </DataGridTemplateColumn.Header>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSeleccionada, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          Style="{StaticResource ModernCheckBox}"
                                                          HorizontalAlignment="Center"
                                                          IsEnabled="{Binding EstaHabilitada, UpdateSourceTrigger=PropertyChanged}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="Fecha"
                                                        Binding="{Binding FechaTexto, Mode=OneWay}"
                                                        Width="120"
                                                        IsReadOnly="True"/>

                                    <DataGridTextColumn Header="Saldo pendiente"
                                                        Width="140"
                                                        IsReadOnly="True">
                                        <DataGridTextColumn.Binding>
                                            <Binding Path="SaldoPendiente"
                                                     Mode="OneWay"
                                                     StringFormat="{}{0:C2}"/>
                                        </DataGridTextColumn.Binding>
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn Header="Monto a aplicar" Width="170">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <xctk:CalculatorUpDown Value="{Binding MontoAplicar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                     CultureInfo="es-MX"
                                                                     FormatString="C2"
                                                                     Minimum="0"
                                                                     Maximum="{Binding SaldoMaximo}"
                                                                     Height="30"
                                                                     HorizontalContentAlignment="Right"
                                                                     IsEnabled="{Binding PuedeEditarMonto}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="Estado"
                                                        Binding="{Binding EstadoDescripcion, Mode=OneWay}"
                                                        Width="*"
                                                        IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#374151"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding EstadoDescripcion}" Value="Pendiente">
                                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EstadoDescripcion}" Value="Actual">
                                                        <Setter Property="Foreground" Value="#F59E0B"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EstadoDescripcion}" Value="Futura">
                                                        <Setter Property="Foreground" Value="#1976D2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EstadoDescripcion}" Value="Futura bloqueada">
                                                        <Setter Property="Foreground" Value="#1976D2"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <TextBlock Text="Selecciona un alumno y taller para mostrar las clases disponibles."
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="#6B7280" FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HayClasesParaPago}" Value="False">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </Border>

                <!-- NOTIFICACIÓN DE TALLERES SIN PAGAR -->
                <Border Grid.Row="3" Background="#FFF3CD" BorderBrush="#FFEAA7" BorderThickness="1"
                        CornerRadius="6" Padding="12" Margin="0,0,0,8"
                        Visibility="{Binding TieneTalleresSinPagar, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding NotificacionTalleresSinPagar}"
                                   Foreground="#856404" FontWeight="SemiBold" VerticalAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- RESUMEN -->
                <Grid Grid.Row="4" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding MensajeValidacion}" Foreground="Tomato" VerticalAlignment="Center" TextWrapping="Wrap"/>
                    <StackPanel Grid.Column="1" Orientation="Vertical" Margin="12,0,12,0" VerticalAlignment="Center">
                        <TextBlock Text="Clases seleccionadas" Foreground="#6B7280" FontSize="12"/>
                        <TextBlock Text="{Binding TotalClasesSeleccionadas}" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center">
                        <TextBlock Text="Total a cobrar" Foreground="#6B7280" FontSize="12"/>
                        <TextBlock Text="{Binding TotalSeleccionado, StringFormat={}{0:C2}}" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>

                <!-- FOOTER -->
                <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancelar" Command="{Binding CancelarPagoClasesCommand}"
                        Style="{StaticResource BotonNormal}" Height="40" MinWidth="120"/>
                    <Button Content="Guardar" Command="{Binding GuardarPagoClasesCommand}"
                        Style="{StaticResource FeaturedButton}" Height="40" MinWidth="140" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>